## "케어플센터" → "C사" 익명화

`src/pages/b2b-center/B2BCenterLanding.tsx`에서 경쟁사 실명을 모두 이니셜 표기로 교체합니다.

### 변경 위치 (4곳)
1. `coreFeatures[0].desc` — "케어플 다운로드 파일 그대로 인식" → "C사 다운로드 파일 그대로 인식"
2. Helmet `<meta description>` — "케어플 엑셀 그대로 이관" → "기존 ERP 엑셀 그대로 이관"
3. Hero 본문 — "케어플 엑셀 파일 하나 올리면" → "기존 ERP 엑셀 파일 하나 올리면"
4. Comparison 섹션 — `02 · vs 케어플센터` → `02 · vs C사`, 테이블 헤더 `케어플센터` → `C사`

가격(₩44,000/₩39,000), 표 항목, 레이아웃은 그대로 유지합니다.
